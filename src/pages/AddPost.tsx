import React, { ReactElement } from 'react';
import HelmetLayout from '../layouts/HelmetLayout';

const AddPost = (): ReactElement => {
	return (
		<HelmetLayout title='AddPost'>
			AddPost
		</HelmetLayout>
	);
};

export default AddPost;