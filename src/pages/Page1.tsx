import React, { ReactElement } from 'react';
import HelmetLayout from '../layouts/HelmetLayout';

const Page1 = (): ReactElement => {
	return (
		<HelmetLayout title='page'>
			Page1
		</HelmetLayout>
	);
};

export default Page1;
